import React from 'react';
import { Text } from "react-native";
import { SafeAreaView } from 'react-native-safe-area-context';

const Landing = () => {

    return (
        <SafeAreaView style={{ padding: 20 }}>
            <Text>Hallo, dit is de landings pagina</Text>
        </SafeAreaView>
    );
};

export default Landing;